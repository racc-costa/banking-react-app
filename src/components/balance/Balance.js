import React from 'react';
import './Balance.css';

const balance = (props) => {
    return (
        <div id="balance">
            Available balance $800.00
        </div>
    )
}

export default balance;